<Window x:Class="lab45.MainWindow"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:lab45"
        mc:Ignorable="d"
        Title="MainWindow" WindowState="Normal">
    
    <Window.Resources>
        <local:ErrorToBrushConverter x:Key="ErrorToBrushConverter" />
    </Window.Resources>

    <Grid Background="Lavender" >
        <Border MinWidth="400" MinHeight="500" Margin="20" Padding="30" Background="White" VerticalAlignment="Center" CornerRadius="15">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="1.5*" />
                    <RowDefinition Height="*" />
                    <RowDefinition Height="0.75*" />
                    <RowDefinition Height="*" />
                    <RowDefinition Height="*" />
                    <RowDefinition Height="*" />
                    <RowDefinition Height="*" />
                    <RowDefinition Height="*" />
                    <RowDefinition Height="1.5*" />
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0" Text="Авторизация" Margin="0 0 0 5" FontSize="50" FontFamily="Arial"/>

                <Grid Grid.Row="1">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <Button x:Name="Register" Grid.Column="0" FontSize="15" Content="Регистрация" Command="{Binding RegisterTabCommand}" />
                    <Button x:Name="Vhod" Grid.Column="1" FontSize="15" Content="Вход" Style="{StaticResource MaterialDesignFlatButton}" Command="{Binding LoginTabCommand}" />
                </Grid>

                <TextBlock Grid.Row="2" Text="*-обязательные поля для заполнения" Margin="0 0 0 10" Style="{DynamicResource MaterialDesignCaptionTextBlock}"/>

                <!-- Фамилия -->
                <TextBox Grid.Row="3"
                         Text="{Binding Firstname, UpdateSourceTrigger=PropertyChanged, ValidatesOnDataErrors=True, NotifyOnValidationError=True}"
                         Validation.ErrorTemplate="{DynamicResource ErrorTemplate}"
                         materialDesign:HintAssist.Hint="Введите фамилию*"
                         Margin="0 0 0 5"
                         FontSize="20"
                         Style="{DynamicResource MaterialDesignFloatingHintTextBox}"
                         Background="{Binding (Validation.HasError), RelativeSource={RelativeSource Self}, Converter={StaticResource ErrorToBrushConverter}}"
                         ToolTip="{Binding (Validation.Errors)[0].ErrorContent, RelativeSource={RelativeSource Self}}" />

                <!-- Имя -->
                <TextBox Grid.Row="4"
                         Text="{Binding Name, UpdateSourceTrigger=PropertyChanged, ValidatesOnDataErrors=True, NotifyOnValidationError=True}"
                         Validation.ErrorTemplate="{DynamicResource ErrorTemplate}"
                         materialDesign:HintAssist.Hint="Введите имя*"
                         Margin="0 0 0 5"
                         FontSize="20"
                         Style="{DynamicResource MaterialDesignFloatingHintTextBox}"
                         Background="{Binding (Validation.HasError), RelativeSource={RelativeSource Self}, Converter={StaticResource ErrorToBrushConverter}}"
                         ToolTip="{Binding (Validation.Errors)[0].ErrorContent, RelativeSource={RelativeSource Self}}" />

                <!-- Номер телефона -->
                <TextBox Grid.Row="5"
                         Text="{Binding Number, UpdateSourceTrigger=PropertyChanged, ValidatesOnDataErrors=True, NotifyOnValidationError=True}"
                         Validation.ErrorTemplate="{DynamicResource ErrorTemplate}"
                         materialDesign:HintAssist.Hint="Введите номер телефона*"
                         Margin="0 0 0 5"
                         FontSize="20"
                         Style="{DynamicResource MaterialDesignFloatingHintTextBox}"
                         Background="{Binding (Validation.HasError), RelativeSource={RelativeSource Self}, Converter={StaticResource ErrorToBrushConverter}}"
                         ToolTip="{Binding (Validation.Errors)[0].ErrorContent, RelativeSource={RelativeSource Self}}" />

                <!-- Пароль -->
                <PasswordBox x:Name="TBpass"
                             Grid.Row="6"
                             materialDesign:HintAssist.Hint="Введите пароль*"
                             Margin="0 5 0 5"
                             FontSize="20"
                             materialDesign:HintAssist.IsFloating="True"
                             Background="{Binding Password, Converter={StaticResource ErrorToBrushConverter}, Mode=OneWay}"
                             ToolTip="{Binding PasswordError}"
                             PasswordChanged="PasswordBox_PasswordChanged" />

                <!-- Email -->
                <TextBox Grid.Row="7"
                         Text="{Binding Email, UpdateSourceTrigger=PropertyChanged, ValidatesOnDataErrors=True, NotifyOnValidationError=True}"
                         Validation.ErrorTemplate="{DynamicResource ErrorTemplate}"
                         materialDesign:HintAssist.Hint="Email"
                         FontSize="20"
                         Style="{DynamicResource MaterialDesignFloatingHintTextBox}"
                         Background="{Binding (Validation.HasError), RelativeSource={RelativeSource Self}, Converter={StaticResource ErrorToBrushConverter}}"
                         ToolTip="{Binding (Validation.Errors)[0].ErrorContent, RelativeSource={RelativeSource Self}}" />

                <!-- Кнопка Создать аккаунт -->
                <Button Grid.Row="8"
                        Content="Создать аккаунт"
                        FontSize="30"
                        MinHeight="60"
                        Command="{Binding RegisterCommand}"
                        IsEnabled="True" />
            </Grid>
        </Border>
    </Grid>
</Window>
<!--    xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"

    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"

    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"

    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"

    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"

    xmlns:local="clr-namespace:lab45"

    xmlns:vm="clr-namespace:lab45.vm"

    mc:Ignorable="d"

   

    Title="MainWindow"   WindowState="Normal">

\<Grid Background="Lavender" \>

  

    \<Border MinWidth="400" MinHeight="500" Margin="20" Padding="30" Background="White" VerticalAlignment="Center" CornerRadius="15"\>

        \<Grid\>

        \<Grid.RowDefinitions\>

            \<RowDefinition Height="1.5*"\>\</RowDefinition\>

            \<RowDefinition Height="*"\>\</RowDefinition\>

            \<RowDefinition Height="0.75*"\>\</RowDefinition\>

               

                \<RowDefinition Height="*"\>\</RowDefinition\>

                \<RowDefinition Height="*"\>\</RowDefinition\>

                \<RowDefinition Height="*"\>\</RowDefinition\>

                \<RowDefinition Height="*"\>\</RowDefinition\>

                \<RowDefinition Height="*"\>\</RowDefinition\>

                \<RowDefinition Height="1.5*"\>\</RowDefinition\>

        \</Grid.RowDefinitions\>

       

        \<TextBlock Grid.Row="0" Text="Авторизация"  Margin="0 0 0 5" FontSize="50" FontFamily="Arial" \>\</TextBlock\>

            \<Grid Grid.Row="1" \>

                \<Grid.ColumnDefinitions\>

                    \<ColumnDefinition Width="*"/\>

                    \<ColumnDefinition Width="*"/\>

                \</Grid.ColumnDefinitions\>

            

            \<Button x:Name="Register" Grid.Column="0" FontSize="15" Content="Регистрация" Click="Button_Click" \>\</Button\>

                \<Button x:Name="Vhod" Grid.Column="1"  FontSize="15"  Content="Вход"  Style="{StaticResource MaterialDesignFlatButton}" Click="Button_Click_1"\>\</Button\>

            \</Grid\>

            \<TextBlock Grid.Row="2" Text="*-обязательные поля для заполнения" Margin="0 0 0 10" Style="{DynamicResource MaterialDesignCaptionTextBlock }"\>\</TextBlock\>

            \<TextBox Validation.ErrorTemplate="{DynamicResource ErrorTemplate}" x:Name="TBfamilia" Grid.Row="3" materialDesign:HintAssist.Hint="Введите фамилию*" Margin="0 0 0 5"  FontSize="20" Style="{DynamicResource MaterialDesignFloatingHintTextBox}" Text="" TextChanged="TBfamilia_TextChanged"\>\</TextBox\>

            \<TextBox x:Name="TBname" Grid.Row="4"  materialDesign:HintAssist.Hint="Введите имя*"  Margin="0 0 0 5" FontSize="20" Style="{DynamicResource MaterialDesignFloatingHintTextBox}" Text="" TextChanged="{}"\>\</TextBox\>

            \<TextBox x:Name="TBnumber"  Grid.Row="5" materialDesign:HintAssist.Hint="Введите номер телефона*"   Margin="0 0 0 5" FontSize="20" Style="{DynamicResource MaterialDesignFloatingHintTextBox}" TextChanged="TBnumber_TextChanged"\>\</TextBox\>

            \<PasswordBox x:Name="TBpass" Grid.Row="6" materialDesign:HintAssist.Hint="Введите пароль*"  materialDesign:HintAssist.IsFloating="True"  Margin="0 5 0 5" FontSize="20"  PasswordChanged="TBpass_PasswordChanged"\>\</PasswordBox\>

            \<TextBox x:Name="TBmail" Grid.Row="7" materialDesign:HintAssist.Hint="Email"  FontSize="20" Style="{DynamicResource MaterialDesignFloatingHintTextBox}" TextChanged="TBmail_TextChanged"\>\</TextBox\>

            \<Button  Grid.Row="8" Content="Создать аккаунт" FontSize="30" MinHeight="60" Click="Button_Reg_Click_2"\>

               

            \</Button\>

        \</Grid\>

    \</Border\>

    

\</Grid\> -->