<Window x:Class="flower.Views.Login1"
           xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:flower.Views"
        mc:Ignorable="d"
        Title="Login1" >
    <Grid Background="Lavender">

        <Border MinWidth="400" MinHeight="400" Margin="20" Padding="30" Background="White" VerticalAlignment="Center" CornerRadius="15">
            <Grid>
               
                <Grid.RowDefinitions>
                    <RowDefinition Height="0.5*"></RowDefinition>
                    <RowDefinition Height="*"></RowDefinition>

                    <RowDefinition Height="*"></RowDefinition>
                    <RowDefinition Height="*"></RowDefinition>
                    <RowDefinition Height="*"></RowDefinition>
                    <RowDefinition Height="2*"></RowDefinition>

                    <RowDefinition Height="*"></RowDefinition>
                </Grid.RowDefinitions>
                <Button  Grid.Row="0" x:Name="LanguageToggleButton" Content="{DynamicResource Language}" Click="LanguageToggleButton_Click" Width="90" Height="30" HorizontalAlignment="Right"  Style="{DynamicResource MaterialDesignFlatButton}"/>
                <TextBlock Grid.Row="1" Text="{DynamicResource Autorisation}"  Margin="0 0 0 5" FontSize="50" FontFamily="Arial" ></TextBlock>
                <Grid Grid.Row="2" >
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <Button x:Name="Register" Grid.Column="0" FontSize="15" Content="{DynamicResource Register}"  Style="{StaticResource MaterialDesignFlatButton}"></Button>
                    <Button x:Name="Vhod" Grid.Column="1"  FontSize="15"  Content="{DynamicResource Login}"></Button>
                </Grid>

                <TextBox x:Name="TBnumberAu" Grid.Row="3" materialDesign:HintAssist.Hint="{DynamicResource Phonenumber}"  Margin="0 0 0 5" FontSize="20" Style="{DynamicResource MaterialDesignFloatingHintTextBox}" ></TextBox>
                <PasswordBox x:Name="TBpassAu" Grid.Row="4" materialDesign:HintAssist.Hint="{DynamicResource Password}"  materialDesign:HintAssist.IsFloating="True"  Margin="0 5 0 5" FontSize="20" ></PasswordBox>

                <Button  Grid.Row="5" Content="{DynamicResource Enter}" Command="{Binding LoginCommand}" FontSize="30" MinHeight="70" Margin=" 0 10 0 10"  ></Button>
                <Button  Grid.Row="6" Content="{DynamicResource AdminEnter}" FontSize="15"  HorizontalAlignment="Left" Style="{DynamicResource MaterialDesignToolButton}" Height="32" Margin="0,7,0,0" VerticalAlignment="Top" ></Button>
            </Grid>
        </Border>


    </Grid>
</Window>
