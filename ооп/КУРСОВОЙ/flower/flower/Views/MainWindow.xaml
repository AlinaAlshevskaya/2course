<Window x:Class="flower.Views.MainWindow"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
       xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
       xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
       xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        
        Title="{DynamicResource Title}"
      
          xmlns:local="clr-namespace:flower.Views">

    <Window.Resources>
        <Style x:Key="TextBoxInError" TargetType="TextBox">
            <Style.Triggers>
                <Trigger Property="Validation.HasError" Value="True">
                    <Setter Property="ToolTip"
                  Value="{Binding RelativeSource={RelativeSource Self}, Path=(Validation.Errors)[0].ErrorContent}"/>
                    <Setter Property="BorderBrush" Value="Pink"></Setter>
                    <Setter Property="Background" Value="Pink"/>
                </Trigger>
            </Style.Triggers>
        </Style>
        <Style x:Key="PasswordBoxInError" TargetType="PasswordBox">
            <Style.Triggers>
                <Trigger Property="Validation.HasError" Value="True">
                    <Setter Property="ToolTip"
                  Value="{Binding RelativeSource={RelativeSource Self}, Path=(Validation.Errors)[0].ErrorContent}"/>
                    <Setter Property="BorderBrush" Value="Pink"></Setter>
                    <Setter Property="Background" Value="Pink"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Grid Background="Lavender" >

        <Border MinWidth="400" MinHeight="550" Margin="20" Padding="30" Background="White" VerticalAlignment="Center" CornerRadius="15" Cursor="">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="0.5*"></RowDefinition>
                    <RowDefinition Height="1.5*"></RowDefinition>
                    <RowDefinition Height="*"></RowDefinition>
                    <RowDefinition Height="0.75*"></RowDefinition>
                    <RowDefinition Height="*"></RowDefinition>
                    <RowDefinition Height="*"></RowDefinition>
                    <RowDefinition Height="*"></RowDefinition>
                    <RowDefinition Height="*"></RowDefinition>
                    <RowDefinition Height="*"></RowDefinition>
                    <RowDefinition Height="1.5*"></RowDefinition>
                </Grid.RowDefinitions>
                <Button  Grid.Row="0" x:Name="LanguageToggleButton" Content="{DynamicResource Language}" Click="LanguageToggleButton_Click" Width="90" Height="30" HorizontalAlignment="Right"  Style="{DynamicResource MaterialDesignFlatButton}"/>
                <TextBlock Grid.Row="1" Text="{DynamicResource Autorisation}"  Margin="0 0 0 5" FontSize="50" FontFamily="Arial" ></TextBlock>
                <Grid Grid.Row="2" >
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Button x:Name="Register" Grid.Column="0" FontSize="15" Content="{DynamicResource Register}"  ></Button>
                    <Button x:Name="Vhod"  Command="{Binding OpenLoginWindowCommand}"  Grid.Column="1"  FontSize="15"  Content="{DynamicResource Login}"  Style="{StaticResource MaterialDesignFlatButton}" ></Button>
                </Grid>
                <TextBlock Grid.Row="3" Text="{DynamicResource RegFields}" Margin="0 0 0 10" Style="{DynamicResource MaterialDesignCaptionTextBlock }"></TextBlock>
                <TextBox  Text="{Binding FamilyName, UpdateSourceTrigger=PropertyChanged, ValidatesOnNotifyDataErrors=True}"  x:Name="TBfamilia" Grid.Row="4" materialDesign:HintAssist.IsFloating="True" materialDesign:HintAssist.Hint="{DynamicResource FirstName}" Margin="0 0 0 5"  FontSize="20" Style="{DynamicResource MaterialDesignFloatingHintTextBox}"></TextBox>
                <TextBox x:Name="TBname" Grid.Row="5"  materialDesign:HintAssist.Hint="{DynamicResource Name}" materialDesign:HintAssist.IsFloating="True" Margin="0 0 0 5" FontSize="20" Style="{DynamicResource MaterialDesignFloatingHintTextBox}" Text="{Binding Name, UpdateSourceTrigger=PropertyChanged, ValidatesOnNotifyDataErrors=True}"></TextBox>
                <TextBox  Text="{Binding PhoneNumber, UpdateSourceTrigger=PropertyChanged, ValidatesOnNotifyDataErrors=True}" x:Name="TBnumber"  Grid.Row="6" materialDesign:HintAssist.Hint="{DynamicResource Phonenumber}" materialDesign:HintAssist.IsFloating="True"   Margin="0 0 0 5" FontSize="20" Style="{DynamicResource MaterialDesignFloatingHintTextBox}"></TextBox>
                <PasswordBox x:Name="TBpass" Grid.Row="7" materialDesign:HintAssist.Hint="{DynamicResource Password}"  materialDesign:HintAssist.IsFloating="True"  Margin="0 5 0 5" FontSize="20" PasswordChanged="PasswordBox_PasswordChanged"></PasswordBox>
                <TextBox x:Name="TBmail" Grid.Row="8" materialDesign:HintAssist.Hint="Email"  FontSize="20" Style="{DynamicResource MaterialDesignFloatingHintTextBox}" materialDesign:HintAssist.IsFloating="True" Text="{Binding Email, UpdateSourceTrigger=PropertyChanged, ValidatesOnNotifyDataErrors=True}" ></TextBox>
                <Button  Grid.Row="9" Content="{DynamicResource CreateAcc}" FontSize="30" MinHeight="60" Command="{Binding CreateAccountCommand}">

                    </Button>  
            </Grid>
        </Border>
    </Grid>
</Window>

