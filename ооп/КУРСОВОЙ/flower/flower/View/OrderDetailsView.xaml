<Window x:Class="flower.View.OrderDetailsView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:flower.View"
        mc:Ignorable="d"
        Title="{DynamicResource OrderDetails_WindowTitle}" Height="800" Width="800"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize">

    <StackPanel Margin="20" Orientation="Vertical">

        <!-- Title -->
        <TextBlock Text="{DynamicResource OrderDetails_WindowTitle}" FontSize="20" FontWeight="Bold" Margin="0,0,0,10" />

        <!-- Order Information Section -->
        <GroupBox Header="{DynamicResource OrderDetails_OrderInformation}" Margin="0,0,0,10" >
            <StackPanel Margin="10">
                <TextBlock Text="{Binding Order.OrderId}" FontWeight="Bold" />
                <TextBlock Text="{Binding Order.User.UserName}" />
                <TextBlock Text="{Binding Order.TotalAmount}" />
                <TextBlock Text="{Binding Order.OrderStatus}" />
            </StackPanel>
        </GroupBox>

        <!-- Feedback Section -->
        <GroupBox Header="{DynamicResource OrderDetails_LeaveFeedback}" Grid.Row="2">
            <StackPanel Margin="10">
                <TextBlock Text="{DynamicResource OrderDetails_YourFeedback}" />
                <TextBox Text="{Binding UserReview}" AcceptsReturn="True" Height="100" Margin="0,5,0,10" 
                         VerticalScrollBarVisibility="Auto" />
                <TextBlock Text="{DynamicResource OrderDetails_Rating}"></TextBlock>
                <TextBox Text="{Binding UserRating}"></TextBox>
                <Button Content="{DynamicResource OrderDetails_SaveReviewButton}" Margin="20,10,0,0" Cursor="Hand" Command="{Binding  SaveReviewCommand}"/>
            </StackPanel>
        </GroupBox>

        <GroupBox Header="{DynamicResource OrderDetails_NewStatus}" x:Name="SetStatus" Grid.Row="3" Visibility="Collapsed">
            <StackPanel>
                <TextBlock Text="{DynamicResource OrderDetails_NewStatus}"></TextBlock>
                <TextBox Text="{Binding NewStatus}" AcceptsReturn="True" Height="100" Margin="0,5,0,10" 
                         VerticalScrollBarVisibility="Auto"></TextBox>
                <Button Content="{DynamicResource OrderDetails_SaveStatusButton}" Margin="20,0,0,0" Cursor="Hand" Command="{Binding  SaveStatusCommand}"/>
            </StackPanel>

        </GroupBox>
    </StackPanel>
</Window>