<Window x:Class="flower.View.CartView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:flower.View"
        xmlns:iconPacks="http://metro.mahapps.com/winfx/xaml/iconpacks"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        mc:Ignorable="d"
        xmlns:mdl="clr-namespace:Models;assembly=Models"
        Title="{DynamicResource Cart_WindowTitle}" Height="700" Width="700">

    <Border CornerRadius="15" Background="#f5f5f5" BorderBrush="#E0E0E0" BorderThickness="1" Padding="0">

        <DockPanel LastChildFill="True">

            <!-- Top panel with close button -->
            <StackPanel DockPanel.Dock="Top" Orientation="Horizontal" Height="56" VerticalAlignment="Center" Background="#7C4DFF">

                <Button Width="38" Height="38" HorizontalAlignment="Right" Style="{DynamicResource MaterialDesignIconButton}" Click="MoreCloseClick">
                    <iconPacks:PackIconMaterial Kind="Close" Foreground="White" />
                </Button>

                <TextBlock Text="{DynamicResource Cart_WindowTitle}" VerticalAlignment="Center" FontSize="18" Foreground="White" FontWeight="Medium" Margin="10,0" />

                <!-- Action Buttons -->
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="30,0,0,0">
                    <Button Content="{DynamicResource Cart_OrderButton}" 
                            Command="{Binding CheckOutCommand}" 
                            Style="{DynamicResource MaterialDesignContainedButton}" 
                            Background="#7C4DFF" Foreground="White" 
                            Width="110" Height="36" Margin="0,0,10,0" 
                            Cursor="Hand"
                            FontSize="16"/>

                    <Button Content="{DynamicResource Cart_CancelButton}" 
                            Click="MoreCloseClick"
                            Style="{DynamicResource MaterialDesignOutlinedButton}" 
                            Width="88" Height="36" 
                            Foreground="White"
                            FontSize="16"
                            Cursor="Hand" />
                </StackPanel>

                <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="20,0,0,0">
                    <TextBlock Foreground="White" FontSize="16" Text="{DynamicResource Cart_TotalPrice}"  />
                    <TextBlock Text="{Binding TotalCart,UpdateSourceTrigger=PropertyChanged}" Width="50" Margin="10,0,0,0" Foreground="White" FontSize="16"/>
                </StackPanel>
            </StackPanel>

            <Grid Margin="20,10,20,0" Background="Transparent">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <!-- ScrollViewer to contain items -->
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <StackPanel>
                        <!-- Binding each product to individual UI elements -->
                        <ItemsControl ItemsSource="{Binding Cart}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate DataType="{x:Type mdl:Product}">
                                    <Border CornerRadius="10" BorderBrush="#E0E0E0" BorderThickness="1" Margin="5" Background="White" Width="400">
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="100" />
                                                <ColumnDefinition Width="*" />
                                            </Grid.ColumnDefinitions>

                                            <Grid.RowDefinitions>
                                                <RowDefinition Height="Auto" />
                                                <RowDefinition Height="Auto" />
                                                <RowDefinition Height="Auto" />
                                                <RowDefinition Height="*" />
                                                <RowDefinition Height="Auto" />
                                            </Grid.RowDefinitions>

                                            <Image Source="{Binding PhotoPath}" Width="80" Height="80" Stretch="Uniform" Grid.Column="0" Grid.Row="0" Margin="10" />
                                            <StackPanel Grid.Column="1" Grid.Row="0" Margin="10">
                                                <TextBlock Text="{Binding ProductName}" FontWeight="Bold" FontSize="16" />
                                                <TextBlock Text="{Binding Price, StringFormat='{}{0:R}'}" FontWeight="Medium" FontSize="14" Margin="0,5,0,0" />
                                                <TextBlock Text="{DynamicResource Cart_OrderedQuantity}" FontWeight="Medium" Margin="0,5,0,0" />
                                                <TextBlock Text="{Binding Amount}" FontWeight="Bold" Margin="0,5,0,0" />
                                            </StackPanel>

                                            <Button Content="{DynamicResource Cart_RemoveButton}" CommandParameter="{Binding}" Command="{Binding DataContext.RemoveCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}" Width="85" Margin="10" Grid.ColumnSpan="2" Grid.Row="4" HorizontalAlignment="Right" />
                                        </Grid>
                                    </Border>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </StackPanel>
                </ScrollViewer>
            </Grid>
        </DockPanel>
    </Border>
</Window>