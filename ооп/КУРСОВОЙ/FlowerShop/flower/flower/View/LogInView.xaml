<Window x:Class="flower.View.LogInView"
           xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:flower.View"
        mc:Ignorable="d"
        Title="{DynamicResource LogInSignUpTitle}" >
    <Grid Background="Lavender">

        <Border MinWidth="400" MinHeight="400" Margin="20" Padding="30" Background="White" VerticalAlignment="Center" CornerRadius="15">
            <Grid>

                <Grid.RowDefinitions>
                    <RowDefinition Height="0.5*"></RowDefinition>
                    <RowDefinition Height="*"></RowDefinition>

                    <RowDefinition Height="*"></RowDefinition>
                    <RowDefinition Height="*"></RowDefinition>
                    <RowDefinition Height="*"></RowDefinition>
                    <RowDefinition Height="2*"></RowDefinition>

                    <RowDefinition Height="*"></RowDefinition>
                </Grid.RowDefinitions>
                <Button  Grid.Row="0" x:Name="LanguageToggleButton" Content="English" Click="ChangeLangClick" Width="90" Height="30" HorizontalAlignment="Right"  Style="{DynamicResource MaterialDesignFlatButton}"/>
                <TextBlock Grid.Row="1" Text="{DynamicResource AuthorisationTitle}"  Margin="0 0 0 5" FontSize="50" FontFamily="Arial" ></TextBlock>
                <Grid Grid.Row="2" >
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <Button x:Name="Register" Grid.Column="0" FontSize="15" Content="{DynamicResource RegisterButtonName}"  Style="{StaticResource MaterialDesignFlatButton}" Click="Register_Click"></Button>
                    <Button x:Name="Vhod" Grid.Column="1"  FontSize="15"  Content="{DynamicResource LogInButtonName}"></Button>
                </Grid>

                <TextBox Text="{Binding  PhoneNumber, UpdateSourceTrigger=PropertyChanged}" x:Name="TBnumberAu" Grid.Row="3" materialDesign:HintAssist.Hint="{DynamicResource PhonrNumberTitle}"  Margin="0 0 0 5" FontSize="20" Style="{DynamicResource MaterialDesignFloatingHintTextBox}" ></TextBox>
                <PasswordBox PasswordChanged="PasswordBox_PasswordChanged"  x:Name="TBpassAu" Grid.Row="4" materialDesign:HintAssist.Hint="{DynamicResource PasswordTitle}"  materialDesign:HintAssist.IsFloating="True"  Margin="0 5 0 5" FontSize="20" ></PasswordBox>

                <Button  Grid.Row="5" Content="{DynamicResource EnterLoginButtonName}" Command="{Binding LogInCommand}" FontSize="30" MinHeight="70" Margin=" 0 10 0 10" ></Button>
                
            </Grid>
        </Border>


    </Grid>
</Window>
